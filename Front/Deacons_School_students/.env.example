# Laravel API (no trailing slash).
# Dev: leave unset — the app uses this Vite origin + /api (proxied to http://127.0.0.1:8000).
# Run from Backend: php artisan serve
# Override if needed:
# VITE_API_URL=http://127.0.0.1:8000/api
