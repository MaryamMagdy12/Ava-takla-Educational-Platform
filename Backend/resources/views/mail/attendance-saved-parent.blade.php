{{ $parentGreeting }}

سلام الرب معكم.

نود إعلامكم بأنه تم تسجيل حضور أبنائكم في مدرسة الشمامسة — كنيسة الأنبا تكلا هيمانوت — وفق البيانات التالية:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
بيانات الحصة
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

التاريخ: {{ $heldOn }}
المرحلة / الفصل: {{ $levelLabel }}
@if(! empty($sessionTitle))
عنوان الحصة: {{ $sessionTitle }}
@endif

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تفاصيل الحضور
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

@foreach ($lines as $line)
• {{ $line['name'] }} — كود الطالب: {{ $line['code'] }} — الحالة: {{ $line['state'] }}
@endforeach

@if(! empty($sessionNotes))
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ملاحظات المعلّم
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{{ $sessionNotes }}
@endif

هذه رسالة تلقائية بعد حفظ الحضور. لأي استفسار، يرجى التواصل مع إدارة المدرسة.

بالصلوات والمحبة،
إدارة مدرسة الشمامسة
كنيسة الأنبا تكلا هيمانوت
